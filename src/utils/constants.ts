/** Activity types for which power bests / power curve are computed. */
export const POWER_BEST_ACTIVITY_TYPES = ["Ride", "VirtualRide"];

/** Activity types that use pace-based rTSS (running). */
export const RUN_ACTIVITY_TYPES = ["Run", "VirtualRun"];

/** Activity types that use pace-based sTSS (swimming). */
export const SWIM_ACTIVITY_TYPES = ["Swim"];
